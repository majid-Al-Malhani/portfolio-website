# * قائمة الخدمات التي يتكون منها المشروع
services:
  # * اسم الخدمة، وتختاره أنت
  web:
    # * بناء الصورة من Dockerfile في المجلد الحالي
    build: .
    # ? بديل build لو أردت صورة جاهزة: image: nginx:alpine
    ports:
      # * ربط المنفذ 8080 في جهازك بالمنفذ 80 في الحاوية
      - "8080:80"
